apiVersion: v1
kind: Pod
metadata:
  name: nginx-pv-lab7-nfs
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
      - mountPath: "/sample/data"
        name: pv-volume-lab7
  volumes:
    - name: nginx-pv-lab7-nfs
      persistentVolumeClaim:
        claimName: pvc-lab7
